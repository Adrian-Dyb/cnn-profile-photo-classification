# Model weights

The project generated 32 trained model checkpoints (`.pth`).

Weights are excluded from the public repository by default to keep the repository lightweight. If checkpoints need to be published, Git LFS is recommended.

The best observed accuracy was 0.95, reached by several MobileNetV2 configurations.
